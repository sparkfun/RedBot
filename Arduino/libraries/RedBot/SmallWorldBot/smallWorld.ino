void smallWorld()
{
  playNote(noteE3, QN);
  playNote(noteF3, QN);
  
  playNote(noteG3, HN);
  playNote(noteE4, HN);
  
  playNote(noteC4, HN);
  playNote(noteD4, QN);
  playNote(noteC4, QN);
  
  playNote(noteC4, HN);
  playNote(noteB4, HN);
  
  playNote(noteB4, HN);
  playNote(noteD3, QN);
  playNote(noteE3, QN);
  
  playNote(noteF3, HN);
  playNote(noteD4, HN);
  
  playNote(noteB4, HN);
  playNote(noteC4, QN);
  playNote(noteB4, QN);
  
  playNote(noteA4, HN);
  playNote(noteG3, HN);
  
  playNote(noteG3, HN);
  playNote(noteE3, QN);
  playNote(noteF3, QN);
  
  playNote(noteG3, HN);
  playNote(noteC4, QN);
  playNote(noteD4, QN);
  
  playNote(noteE4, HN);
  playNote(noteF4, QN);
  playNote(noteC4, QN);
  
  playNote(noteA4, HN);
  playNote(noteD4, QN);
  playNote(noteE4, QN);
  
  playNote(noteF4, HN);
  playNote(noteE4, QN);
  playNote(noteD4, QN);
}
  
